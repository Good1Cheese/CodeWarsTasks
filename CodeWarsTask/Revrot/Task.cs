namespace CodeWarsTask.Revrot
{
    public class Task
    {

        //public static void RevRot(string str, int sz)
        //{
        //    bool isSzLowerZeroOrGreaterStr = sz <= 0 || sz > str.Length;
        //    if (isSzLowerZeroOrGreaterStr || string.IsNullOrEmpty(str)) { Console.WriteLine(""); }
        //    string[] chunks = CreateChunks(str, sz);

        //    foreach (string strChunk in chunks)
        //    {
        //        int intChunk = int.Parse(strChunk);
        //        if ()
        //    }

        //}

        //static string[] CreateChunks(string str, int sz)
        //{
        //    var chunks = new List<string>();
        //    int startIndex = 0;
        //    for (int i = 0; i < str.Length; i++)
        //    {
        //        int charIndex = (i + 1);
        //        if ((charIndex % sz) == 0)
        //        {
        //            string chunk = str.Substring(startIndex, sz);
        //            chunks.Add(chunk);
        //            startIndex = charIndex;
        //        }
        //    }
        //    return chunks.ToArray();
        //}
    }
}